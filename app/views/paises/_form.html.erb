<%= form_for(@pais) do |f| %>
  <% if @pais.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pais.errors.count, "error") %> prohibited this pais from being saved:</h2>
      <p>
        <%= f.label :nombre %>
        <%= f.text_field :nombre %>
      </p>
      <p>
        <%= f.label :coordenadas %>
        <%= f.text_field :coordenadas %>
      </p>
      <ul>
      <% @pais.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
